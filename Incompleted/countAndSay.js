/**
 * @param {number} n
 * @return {string}
 */
var countAndSay = function (n) {
	let num = parseInt(n);
	if (num >= 2) {
        
	} else {
		return num.toString();
	}
};

console.log(countAndSay(4));
